# BRIEF — session terminal : gros commit de la passe "cherche encore" (bug de bannière corrigé, README/vidéo/deck mis à jour, dashboard à republier)

*À coller dans une session terminal lancée depuis `~/hindsight-alpha`. Fait suite à `BRIEF_RECHERCHE_HACKATHON_DEJA_FAITE.md` — son verdict (commits `5bfa50b`, `0fe900f`, tous poussés) : write-up vérifié propre, bracket/OCO confirmé non supporté sur options, découverte des 11 échecs consécutifs du moniteur (DarkWake). Ce brief REMPLACE une version précédente du même nom écrite plus tôt aujourd'hui — le périmètre a grandi entre-temps suite à une demande explicite de Spap ("cherche encore, explore ligne par ligne, contrôle et double-contrôle, reviens avec une solution"). Ne traite que celui-ci, la version précédente n'a jamais été poussée.*

*Mis à jour 25/08 tard : ajout de `submission/Hindsight_Alpha_Deck.pptx` à la liste ci-dessous, suite à une 4e passe "cherche encore" qui a trouvé le deck de slides jamais relu cette session — il affichait encore "0 leaks" alors que `hindsight_guard` a détecté un vrai désaccord sur XLK et le refuse en live (contradiction directe avec le write-up/README/vidéo déjà corrigés). Détail complet dans `PLAN_SPRINT.md`, section "submission/Hindsight_Alpha_Deck.pptx n'avait jamais été relu cette session".*

*Mis à jour 25/08, encore plus tard : Spap a remarqué qu'aucun garde-fou ne tournait sur ce projet. Deux nouveaux fichiers untracked ajoutés : `garde_fou.py` (contrôle avant publication, construit avec le skill `garde-fou-generique` — 4 contrôles nés d'erreurs réelles de cette session, testé pour de vrai, un faux positif trouvé et corrigé dans la foulée) et `CLAUDE.md` (règle du garde-fou + rappel condensé des contraintes non négociables du sprint). `python3 garde_fou.py` tourne déjà propre sur l'état actuel (verdict 🟡, une seule alerte non bloquante sur le nombre d'équipes du deck, à revérifier à la main avant soumission finale). Détail dans `PLAN_SPRINT.md`, section "garde_fou.py posé".*

*Mis à jour 25/08, une troisième fois : comparaison demandée contre de vrais projets GitHub (gitleaks, pre-commit, danger-js, doc-drift). Deux ajouts : `garde_fou.py` vérifie maintenant l'historique git complet pour `.env.hackathon` (pas juste l'index actuel — inspiré de gitleaks), et un nouveau dossier versionné `githooks/pre-commit` fait tourner `garde_fou.py` automatiquement à chaque commit une fois activé (`git config core.hooksPath githooks` — à faire une fois sur cette machine, la commande ne survit pas au clonage donc chaque nouvelle machine devra la relancer). **Active ce hook toi-même si tu ne l'as pas encore fait** : sans ça, `garde_fou.py` existe mais ne protège rien tant qu'on ne pense pas à le lancer à la main. Détail complet, y compris un bug trouvé et corrigé dans le hook lui-même (`set -e` qui avalait le message d'erreur), dans `PLAN_SPRINT.md`.*

*Mis à jour 25/08, une quatrième fois : Spap a demandé d'aller jusqu'au bout — une vraie source de vérité mécanique, pas une liste noire. `garde_fou.py` a maintenant un 5e contrôle qui recalcule à chaque run, depuis `BACKTEST_RESULTS.md`/`STRATEGY_COMPARISON.md`/`agent.py`/`risk_gates.py`/`vol_strategy.py`, ce que devraient être les plages taux de succès/concentration/trades, le nombre de leaks, l'univers actuel et les seuils de risque — puis compare ça à ce qui est réellement écrit dans les 4 livrables. Testé dans les deux sens (témoin avec 7 chiffres faux → tout bloque avec le bon message ; mêmes chiffres corrects → 🟢) avant d'être déclaré fini. Trois bugs de proximité regex trouvés et corrigés en le testant contre les vrais fichiers — détail dans `PLAN_SPRINT.md`, section "fait le travail jusqu'à la fin". `garde_fou.py` compte maintenant 5 contrôles.*

*Mis à jour 25/08, une cinquième fois : "cherche encore sur GitHub" a trouvé le vrai trou restant — le hook local (`githooks/pre-commit`) ne protège que les machines où il a été activé, et `--no-verify` le contourne par construction. Nouveau fichier untracked : `.github/workflows/garde-fou.yml`, lance `python3 garde_fou.py` sur GitHub même à chaque push/PR vers `main` (avec `fetch-depth: 0`, nécessaire pour que le contrôle de l'historique git fonctionne). Testé en simulant un vrai clone frais — a révélé (sans surprise, une fois compris) que l'état COMMITÉ actuel a encore l'ancien "442" puisque le correctif du deck n'est encore que dans l'arbre de travail : la CI bloquerait un vrai push tant que ce commit n'est pas fait. Une fois poussé sur GitHub, cette CI apparaîtra comme coche verte/rouge visible sur le dépôt public — bon signal de rigueur pour un juge qui parcourt le repo, pas juste une protection interne.*

*Mis à jour 25/08, une sixième fois : plusieurs ajouts la même soirée. (a) `garde_fou.py` a maintenant un 6e contrôle (`requirements.txt` scellé, non bloquant, né d'une demande d'installer un dépôt GitHub externe non vérifié — refusée). (b) Nouveau fichier untracked `githooks/pre-push` : bloque tout `git push --force`/`--force-with-lease` (adapté du principe `/careful` de gstack, comble un trou réel — cette règle était répétée dans CLAUDE.md sans jamais être vérifiée mécaniquement), testé en 7 scénarios sur un dépôt jetable. (c) Section "Vérifier avant d'affirmer" ajoutée à CLAUDE.md. (d) **Une vraie revue croisée à 4 agents (inspirée du plugin officiel Anthropic `/code-review`) lancée sur tout le lot en attente avant ce commit** : deux vrais bugs trouvés et corrigés dans la foulée — un chiffre de concentration erroné pour XLK dans le nouveau tableau de `README.md` (181.6% affiché au lieu de 136.7%, la fenêtre 10j collée à tort à la fenêtre 90j vettée — confirmé indépendamment par deux agents différents), et un commentaire périmé dans `garde_fou.py` qui disait encore "4 contrôles" alors que le code en a 6. Une vraie faille structurelle notée mais PAS corrigée ce soir (juste documentée en commentaire dans `garde_fou.py`) : le contrôle 5 exclut par construction les symboles en fuite (XLK) de ses plages mécaniques — un chiffre faux sur XLK spécifiquement ne serait vu par AUCUN contrôle actuel, exactement le bug qui vient d'être trouvé à la main. Une mine dormante de moins aussi : le motif "442" (contrôle 4) matchait n'importe quel nombre le contenant en sous-chaîne (ex. "4420") — resserré avec les mêmes bornes `(?<!\d)...(?!\d)` déjà utilisées ailleurs dans le fichier. Détail complet des 4 rapports d'agents dans `PLAN_SPRINT.md`. `garde_fou.py` toujours 🟡 après tous ces correctifs, verdict inchangé.*

*Mis à jour 26/08 : ajout purement documentaire, aucun code touché. `PLAN_SPRINT.md` a reçu une entrée sur le vetting de 4 outils externes cités dans une vidéo TikTok (OmniRoute, Headroom, Claude Code Setup, Task Observer) — verdict : aucun installé, OmniRoute refusé (étoiles GitHub à un rythme extrême + le README admet lui-même contourner les CGU de 15 fournisseurs), Headroom mis sous surveillance sans refus net, Claude Code Setup déjà connu fiable, Task Observer seul candidat plausible pour adaptation future. Le même soir, les principes `verification-before-completion` (superpowers) et le hook `githooks/pre-push` de ce projet ont été évalués pour adaptation à trois autres dossiers de CERVEAU (SNIPER, JEU BLOCKCHAIN, claudeverstradingview) — hors périmètre de ce dépôt, journalisé dans leurs propres `JOURNAL_BORD.md`, aucun fichier de hindsight-alpha touché par cette partie-là. Rien de nouveau à vérifier en session terminal pour cette mise à jour — la liste de fichiers modifiés/untracked ci-dessous reste exacte.*

---

## Contexte, honnêtement

`git status` actuel : 10 fichiers modifiés (`.gitignore`, `PLAN_SPRINT.md`, `README.md`, `agent.py`, `backtest.py`, `docs/index.html`, `monitor_exits.py`, `publish_dashboard.py`, `submission/Hindsight_Alpha_Writeup.docx`, `submission/Video_Script.md`, `submission/Hindsight_Alpha_Deck.pptx`) + 5 fichiers/dossiers untracked (ce brief, `garde_fou.py`, `CLAUDE.md`, `githooks/pre-commit`, `.github/workflows/garde-fou.yml`). `HEAD` local == `origin/main` == `0fe900f` (vérifié), donc tout ce qui suit part de la dernière session terminal, rien entre les deux. `py_compile` + `pyflakes` propres sur tout `*.py` (vérifié après coup, pas juste avant) — y compris `garde_fou.py`, lancé pour de vrai plusieurs fois, pas juste compilé.

**Sur `submission/Hindsight_Alpha_Deck.pptx` spécifiquement** : déjà revalidé côté Cowork (`validate.py --original` propre, conversion PDF + inspection visuelle des slides 3 et 8 faite, aucun débordement de texte, `markitdown` recontrôlé après coup — plus aucune trace des anciens chiffres périmés). Rien de plus à vérifier dessus sauf si tu veux un second regard visuel (`python scripts/office/soffice.py --headless --convert-to pdf` puis `pdftoppm`).

### Le morceau le plus important : un vrai bug trouvé en confrontant l'indicateur de santé du dashboard (construit plus tôt aujourd'hui) à de VRAIES données, pas des scénarios inventés

En relisant `monitor_exits.log` pour préparer ce brief, découvert que le moniteur a réellement récupéré à 17:55 UTC et tourne bien depuis (contrôles réussis toutes les 15 min jusqu'à au moins 19:15) — mais `decision_log.jsonl` ne contient toujours QUE les 11 échecs DarkWake comme dernières entrées `exit_monitor`, parce que `monitor_exits.py` n'a jamais écrit dans ce fichier pour un contrôle routinier réussi (design intentionnel, pour ne pas noyer le dashboard de bruit — voir le commentaire déjà présent dans le code avant aujourd'hui). **Conséquence concrète** : la bannière construite ce matin, basée uniquement sur `decision_log.jsonl`, afficherait "🔴 11 consecutive failures" indéfiniment, potentiellement toute la semaine jugée, même longtemps après que le problème soit réglé.

**Corrigé sans toucher au design intentionnel de `decision_log.jsonl`** :
- `monitor_exits.py` : nouveau fichier `monitor_last_run.json` (gitignored), écrit à CHAQUE run sans condition (`_write_last_run_status()`, appelée dans le `finally`, résiste à toute exception).
- `publish_dashboard.py` : le lit (best-effort) et le republie dans `data.json` sous `monitor_status`.
- `docs/index.html` : `renderMonitorHealth()` utilise `monitor_status` en signal primaire, avec repli sur l'ancienne logique si absent (compatibilité avec un `data.json` généré avant ce correctif — donc pas de casse même si republication tarde).
- Ajout non prévu au départ : un contrôle d'heures de marché côté client (`isUsMarketHoursNow()`, fuseau `America/New_York` via `Intl`, DST géré automatiquement) — sans ça, la bannière serait jaune "stale" chaque nuit et chaque week-end.

Vérifié : 3 scénarios Python isolés (écriture/lecture du nouveau fichier, résilience sur chemin non-inscriptible), 10 scénarios JS sur `renderMonitorHealth` (dont le cas exact de l'incident réel — 11 échecs + récupération — qui doit rester vert), 6 scénarios sur `isUsMarketHoursNow` avec timestamps UTC fixes couvrant séance/hors-séance/week-end/DST.

### Le reste : 3 agents dispatchés en parallèle pour l'audit "ligne par ligne" demandé par Spap

- **Audit code** : rien de fonctionnel cassé sur 13 fichiers `.py` relus intégralement. Deux imports morts retirés (`json` dans `agent.py`, `json`+`daily_returns` dans `backtest.py`, confirmés par `pyflakes`).
- **Re-vérification du règlement lablab/Alpaca** : du neuf trouvé — soumission finale doit inclure l'Alpaca account ID (déjà anticipé dans `publish_dashboard.py`, rien à faire) ; vidéo max 5 min format MP4, structure "intro → PDF → fonctionnalités" (script mis à jour) ; **cover image PNG/JPG 16:9 requise, pas encore préparée** ; licence MIT déjà conforme ; "social engagement" noté sur qualité ET engagement généré, pas juste sur le fait de poster. Concurrence plus relevée que prévu mais **personne d'autre repéré ne fait de détection de hindsight-leakage** — différenciation intacte.
- **Audit présentation** : `Video_Script.md` corrigé (occultait le vrai leak XLK au lieu de le montrer comme preuve ; citait un chiffre de l'ancien univers abandonné SPY/QQQ/IWM) ; `README.md` corrigé (le lien du dashboard n'apparaissait **nulle part** dans le fichier — ajouté dès les premières lignes, plus un TL;DR, un tableau de backtest condensé, et une mise à jour honnête de la section Status).

## Ce qu'on te demande

1. `git status` et `git diff --stat` — confirme que les 10 fichiers modifiés + 4 nouveaux (`garde_fou.py`, `CLAUDE.md`, `githooks/pre-commit`, `.github/workflows/garde-fou.yml`) sont les seuls changements (en dehors de `docs/data.json` que l'étape suivante va modifier). Lance aussi `python3 garde_fou.py` une fois pour voir son verdict en conditions réelles de terminal (pas juste dans le bac à sable Cowork) — devrait rendre 🟡 avec une seule alerte (nombre d'équipes du deck à revérifier à la main). Puis `git config core.hooksPath githooks` pour activer le hook automatique sur cette machine (une seule fois, ne survit pas au clonage).
2. **`python publish_dashboard.py`** (sans `--git-push`, juste régénérer localement) contre le compte de **dev** (`.env`) — rafraîchit `docs/data.json` avec le vrai `decision_log.jsonl` actuel ET le nouveau `monitor_status` (qui doit exister maintenant si `monitor_exits.py` a tourné au moins une fois depuis ce commit — sinon `monitor_status` sera `null`, ce qui est géré proprement par le repli automatique de la bannière, pas une erreur).
3. Ouvre `docs/index.html` en local (`python3 -m http.server` depuis `docs/`) et confirme visuellement :
   - la bannière de santé reflète l'état réel (pas coincée sur "11 consecutive failures" si le moniteur a effectivement récupéré),
   - le nouveau bandeau "🛡️ Hindsight leaks caught" apparaît si `decision_log.jsonl` contient bien des refus `hindsight_guard:` (il devrait, XLK est refusé systématiquement),
   - les lignes de refus XLK dans le tableau "Recent Decisions" sont bien surlignées (icône 🛡️, couleur distincte).
4. Reconvertis rapidement `submission/Hindsight_Alpha_Writeup.docx` en PDF — confirme que ça reste une page propre (changement mineur déjà vérifié, juste une double-vérification). Si tu veux, fais la même chose pour `submission/Hindsight_Alpha_Deck.pptx` (PDF + coup d'œil sur les slides 3 et 8) — déjà vérifié côté Cowork, donc facultatif.
5. Si tout est propre : `git add`, un commit qui regroupe ces changements (`docs/data.json` inclus une fois régénéré) — un seul commit ou plusieurs selon ta préférence habituelle pour garder l'historique lisible — puis `git push`.
6. Mets à jour `PLAN_SPRINT.md` avec le résultat réel : la bannière a-t-elle rendu l'état attendu une fois `data.json` rafraîchi, et quel est l'état du moniteur à ce moment précis (toujours sain depuis 17:55, ou un nouvel échec est survenu depuis) ?

## Hors périmètre

- **Ne touche pas à `.env.hackathon` ni au compte `PA3K8MP3MF0U` avant le kickoff du 28/08.**
- **Ne passe jamais `--live` au CLI, ne définis jamais `ALPACA_LIVE_TRADE=true`.**
- **Ne modifie aucun seuil de risque**, ni les seuils de la bannière elle-même (45 min, 3 échecs, fenêtre de marché 9:30-16:00 ET) même si le rendu réel te semble trop bruyant ou pas assez — décision de Spap, pas un ajustement à faire ici.
- **Ne bascule pas la stratégie sur `momentum_strategy.py`** — le README documente maintenant explicitement que `momentum_strategy` passe `hindsight_guard` plus proprement (4/4 vs 3/4), c'est une observation honnête à laisser telle quelle, pas une invitation à changer la stratégie live.
- **Ne prépare pas la cover image** (PNG/JPG 16:9) — nécessite un vrai choix visuel de Spap, pas une tâche de session terminal.
- **Ne poste rien sur X/LinkedIn.**
- **Pas de `git push --force`.**
- **Ne touche à aucun autre dossier de CERVEAU.**

## En fin de séance

Verdict net, point par point :
- **`docs/data.json` régénéré** avec `monitor_status` présent (ou `null` si le moniteur n'a pas encore re-tourné depuis ce commit — dis-le clairement) et bannière confirmée en vrai dans le navigateur : oui ou pas, avec la couleur observée.
- **Bandeau "Hindsight leaks caught" et surlignage XLK visibles** : oui ou pas.
- **Write-up toujours propre** : oui ou pas.
- **Commit(s) poussé(s)** (hash) ou rien à pousser.

Mets à jour `PLAN_SPRINT.md` avant de terminer.

---

**Compte paper trading uniquement — zéro fonds réel engagé.**
